<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>관리자 페이지</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
<div class="container py-5">
    <h2 class="mb-4">관리자 페이지</h2>
    <h4>승인 대기 퀴즈</h4>
    <table class="table table-bordered">
        <thead>
            <tr>
                <th>제목</th><th>카테고리</th><th>승인/거절</th>
            </tr>
        </thead>
        <tbody>
            <% if (pendingQuizzes && pendingQuizzes.length > 0) { %>
                <% pendingQuizzes.forEach((quiz) => { %>
                <tr>
                    <td><%= quiz.title %></td>
                    <td><%= quiz.category %></td>
                    <td>
                        <form action="/admin/approve" method="POST" style="display:inline;">
                            <input type="hidden" name="idx" value="<%= quiz.id %>">
                            <button class="btn btn-success btn-sm">승인</button>
                        </form>
                        <form action="/admin/reject" method="POST" style="display:inline;">
                            <input type="hidden" name="idx" value="<%= quiz.id %>">
                            <button class="btn btn-danger btn-sm">거절</button>
                        </form>
                    </td>
                </tr>
                <% }) %>
            <% } else { %>
                <tr><td colspan="4" class="text-center">대기 중인 퀴즈가 없습니다.</td></tr>
            <% } %>
        </tbody>
    </table>

    <h4 class="mt-5">모든 유저 정보</h4>
    <div style="max-height:300px; overflow-y:auto; border:1px solid #ccc; border-radius:8px; margin-bottom:24px;">
        <table class="table table-bordered mb-0">
            <thead>
                <tr><th>ID</th><th>이름</th><th>이메일</th><th>권한</th></tr>
            </thead>
            <tbody>
                <% users.forEach(user => { %>
                    <tr>
                        <td><%= user.id %></td>
                        <td><%= user.username %></td>
                        <td><%= user.email %></td>
                        <td><%= user.role %></td>
                    </tr>
                <% }) %>
            </tbody>
        </table>
    </div>

    <h4 class="mt-5">모든 퀴즈 정보</h4>
    <div style="max-height:300px; overflow-y:auto; border:1px solid #ccc; border-radius:8px; margin-bottom:24px;">
        <table class="table table-bordered mb-0">
            <thead>
                <tr><th>ID</th><th>제목</th><th>카테고리</th></tr>
            </thead>
            <tbody>
                <% quizzes.forEach(quiz => { %>
                    <tr>
                        <td><%= quiz.id %></td>
                        <td><%= quiz.title %></td>
                        <td><%= quiz.category %></td>
                    </tr>
                <% }) %>
            </tbody>
        </table>
    </div>

    <h4 class="mt-5">공지사항 작성</h4>
    <form action="/admin/notice" method="POST">
        <div class="mb-3">
            <label class="form-label">제목</label>
            <input type="text" class="form-control" name="title" required>
        </div>
        <div class="mb-3">
            <label class="form-label">내용</label>
            <textarea class="form-control" name="content" rows="3" required></textarea>
        </div>
        <button type="submit" class="btn btn-primary">공지 등록</button>
    </form>
</div>
</body>
</html> 