<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>오늘의 퀴즈</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Custom CSS -->
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <!-- Bootstrap 네비게이션 바 -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <div class="container">
            <a class="navbar-brand" href="/">
                <img src="/home.png" alt="로고" class="logo">
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav me-auto">
                    <!-- <li class="nav-item">
                        <a class="nav-link" href="/">홈</a>
                    </li> -->
                    <li class="nav-item">
                        <a class="nav-link" href="/notice">공지사항</a>
                    </li>
                </ul>
                <div class="d-flex">
                    <a href="/login" class="btn btn-outline-light me-2">로그인</a>
                    <a href="/signup" class="btn btn-light">회원가입</a>
                </div>
            </div>
        </div>
    </nav>

    <form action="/search" method="POST" class="search-container">
        <div class="search-box">
            <button class="filter-btn btn btn-secondary">▼</button>
            <input type="text" name="search" class="search-input form-control" placeholder="검색어를 입력하세요.">
            <button type="submit" class="search-btn btn btn-primary">검색</button>
        </div>
    </form>

    <div class="image-grid">
        <% images.forEach(img => { %>
            <div class="image-card card">
                <div class="image-container">
                    <img src="<%= img.src %>" alt="<%= img.title %>" class="card-img-top">
                </div>
                <div class="image-info card-body">
                    <h3 class="card-title"><%= img.title %></h3>
                    <p class="card-text"><%= img.description %></p>
                </div>
            </div>
        <% }) %>
    </div>

    <!-- Bootstrap JS Bundle with Popper -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
