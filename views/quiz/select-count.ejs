<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>문제 수 선택</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #6c5ce7;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
        }

        .back-button {
            position: absolute;
            top: 20px;
            left: 20px;
            background-color: white;
            border: none;
            padding: 8px 16px;
            border-radius: 5px;
            text-decoration: none;
            color: #333;
        }

        .content-container {
            max-width: 800px;
            margin: 50px auto;
            padding: 20px;
            text-align: center;
            flex-grow: 1;
            display: flex;
            flex-direction: column;
            justify-content: center;
        }

        .quiz-image {
            width: 300px;
            height: auto;
            margin: 0 auto 30px;
            display: block;
        }

        .info-box {
            background-color: rgba(44, 41, 81, 0.9);
            padding: 20px;
            border-radius: 10px;
            color: white;
            margin-bottom: 30px;
        }

        .info-box h2 {
            margin-bottom: 10px;
            font-size: 24px;
        }

        .info-box p {
            margin-bottom: 0;
            font-size: 16px;
        }

        .count-buttons {
            display: flex;
            justify-content: center;
            gap: 15px;
            flex-wrap: wrap;
        }

        .count-button {
            background-color: #8c7ae6;
            color: white;
            border: none;
            padding: 12px 24px;
            border-radius: 25px;
            font-size: 16px;
            text-decoration: none;
            transition: transform 0.2s, background-color 0.2s;
        }

        .count-button:hover {
            background-color: #9c88ff;
            transform: translateY(-2px);
            color: white;
        }
    </style>
</head>
<body>
    <a href="/" class="back-button">돌아가기</a>

    <div class="content-container">
        <img src="<%= quiz.thumbnail || '/quiz-logo.png' %>" alt="퀴즈 이미지" class="quiz-image">
        
        <div class="info-box">
            <h2>퀴즈 제목</h2>
            <p>문제 설명</p>
        </div>

        <div class="count-buttons">
            <a href="/quiz/play/<%= quiz.id %>?count=10" class="count-button">>10개 풀기</a>
            <a href="/quiz/play/<%= quiz.id %>?count=20" class="count-button">>20개 풀기</a>
            <a href="/quiz/play/<%= quiz.id %>?count=30" class="count-button">>30개 풀기</a>
            <a href="/quiz/play/<%= quiz.id %>?count=50" class="count-button">>50개 풀기</a>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html> 